## antha run

Run an antha workflow

### Synopsis

Run an antha workflow

```
antha run [flags]
```

### Options

```
      --bundle string                   Input bundle with parameters and workflow together (overrides parameter and workflow arguments)
      --component strings               Uris of remote components ({tcp,go}://...); use multiple flags for multiple components
      --driver strings                  Uris of remote drivers ({tcp,go}://...); use multiple flags for multiple drivers
      --fixVolumes                      Make all volumes sufficient for later uses (default true)
  -h, --help                            help for run
      --ignorePhysicalSimulation        Ignore errors when physically simulating the workflow - use to suppress issues caused by bugs in physical simulations
      --inputPlateTypes strings         Default input plate types (in order of preference)
      --inputPlates strings             File containing input plates
      --layoutSummary string            save a summary of the generated deck layout to the given filename
      --legacyVolumeTracking            Do not track volumes for intermediate components
      --makeTestBundle string           Generate json format bundle for testing and put it here
      --maxPlates int                   Maximum number of plates
      --maxWells int                    Maximum number of wells on a plate
      --mixInstructionFileName string   Name of instructions files to output to for mixes
      --mixSummary string               save a summary of the generated liquidhandling actions to the given filename
      --outputPlateTypes strings        Default output plate types (in order of preference)
      --outputSort                      Sort execution by output - improves tip usage
      --parameters string               Parameters to workflow
      --policyFile string               Design file of custom liquid policies in format of .xlsx JMP file
      --printInstructions               Output the raw instructions sent to the driver
      --residualVolumeWeight float      Residual volume weight
      --runTest                         compare mix instructions and time estimates with results previously generated by using the makeTestBundle flag. 
      --tipTypes strings                Names of permitted tip types
      --useDriverTipTracking            If the driver has tip tracking available, use it
      --withMulti                       Allow use of new multichannel planning - deprecated
      --workflow string                 Workflow definition file
```

### Options inherited from parent commands

```
      --config string   Config file (default is $HOME/.antharun.yaml)
```

### SEE ALSO

* [antha](antha.md)	 - Antha command line tool

###### Auto generated by spf13/cobra on 11-Apr-2019
